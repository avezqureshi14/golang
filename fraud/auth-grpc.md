Phase 1 (today)
✔ implement interceptor + API key

Phase 2
✔ move to JWT between services
use a token manager , token must be generated once per sevice identity session , use token refreshing before token is going to get expired 

Phase 3 (production level)
✔ mTLS or service mesh (Istio)
