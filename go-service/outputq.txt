Slicing behavior (1:26 - 2:09): The creator asks for the output of a slice operation on a fixed-size array, highlighting the common mistake of including the end index in a range slice.
Slice reference property (3:51 - 4:26): After modifying an element in a slice, the creator asks how that change reflects in the original array, testing the knowledge that slices are reference types.
make function and append behavior (6:49 - 7:25): The creator provides a code snippet using make with a specific size and asks what happens when an element is appended to it, clarifying that make initializes the slice with zero-values.
Length and Capacity calculation (10:28 - 11:29): The creator tests the viewer's understanding of how len() and cap() methods work, specifically how capacity grows when a slice is appended.
Index out of range (12:25 - 13:51): The creator asks what happens if you try to assign a value to a high index (e.g., the 60th index) of a slice that has a large capacity but a smaller length, explaining the resulting runtime error.
Function arguments and references (15:13 - 15:45): The creator asks for the output when a slice is passed to a function that modifies an element, emphasizing that because slices are reference types, the changes affect the original data.